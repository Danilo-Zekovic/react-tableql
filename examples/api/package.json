{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "start": "yarn run compile && node ./dist/index.js",
    "dev": "ts-node-dev src/index.ts",
    "container": "docker-compose up -d",
    "seed": "ts-node-dev src/seeds.ts",
    "dockerDevApi": "echo \"-- Starting API --\" && pm2 --name api start yarn --interpreter sh -- dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apollo/server": "^4.3.0",
    "graphql": "^16.6.0",
    "mongoose": "^6.8.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.15",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.9.4"
  }
}
